[package]
name = "models"
version = "0.1.0"
authors = ["AureliaDolo <aurelia.dolo@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0", features = ["derive"] }
crypto = {path = "../crypto"}
flexbuffers = { version = "0.2" }
thiserror = "1.0"
