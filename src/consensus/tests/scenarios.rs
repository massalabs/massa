use super::super::{
    config::ConsensusConfig, consensus_controller::ConsensusController,
    default_consensus_controller::DefaultConsensusController,
};
use super::mock_protocol_controller::{self};

#[tokio::test]
async fn test_xxxxx() {
    let (mut protocol_controller, mut protocol_controler_interface) =
        mock_protocol_controller::new();

    //TODO
}
